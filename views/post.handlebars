<div class="text-center">
    <h2>{{title}}</h2>
    <p>Created by {{username}} on {{format_date date}}</p>
    <p>
        {{content}}
    </p>
    <h3>Comments:</h3>
    {{#each comments as |comment| }}
    <div class="comment">
        <p>{{username}}{{date}}:</p>
        <p>{{comment}}</p>
    </div>
    {{/each}}

</div>