{{#each posts as |post|}}
<div class="row mb-4 project">
    <div class="col-md-5">
        <h2>
            <a href="/post/{{post.title}}"></a>
        </h2>
        <p>
            {{post.}}
        </p>
    </div>
</div>